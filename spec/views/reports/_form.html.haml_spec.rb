require 'spec_helper'

describe "reports/_form" do
  it "renders form partial with correct elements" do
    # render 'reports/_form.html.haml'
    # rendered.should =~ /Upload Attachment/
    # rendered.should =~ /Cancel/
    # rendered.should have_selector 'table'
  end
end
