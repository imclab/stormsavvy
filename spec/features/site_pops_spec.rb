require 'spec_helper'

describe "SitePops" do
  describe "GET /site_pops" do
    it "works! (now write some real specs)" do
      # get site_pops_path
      # response.status.should be(200)
    end
  end
end
