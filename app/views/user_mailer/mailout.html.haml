%h2 Your project alerts
%hr
%ul.projects
  -# TODO: Spec print_sites and move to user mailer helper
  -#= "Alerts for your projects: #{ @user.print_sites }"
  = "Alerts for your projects:"
/ TODO: Make sure site info can render in partial.
/ - @projects.each do |project|
/   %li
/     = project.name
/     %ul.sites
/       - project.sites.each do |site| 
/         %li.chance-of-rain
/           = "#{site.name} => chance of rain = #{site.chance_of_rain}%"
