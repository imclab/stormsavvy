.container
  .row
    .span12
      %h1 Active Sites
  .row
    .span12
      %ul
        - @projects.each do |project|
          - project.sites.each do |site|
            %li= link_to site.name, project_site_path(project, site)