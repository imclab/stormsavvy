-begin
  - unless @sites.blank?
    - @sites.each do |site|
      .row-fluid.widget.bottom-10
        .inner
          %h3
            = link_to site.name, site_path(site)
            = link_to 'Remove Site', site, method: 'delete', class: 'btn'
        .cf
          %ul
            %li
              %b Last Updated:
              = site.updated_at
            %li
              %b Site ID:
              = site.id
            %li
              %b Site Lat/Long:
              = site.lat
              = site.long
            %li
              %b Site Zipcode:
              = site.zipcode
            %br
  - else
    %b No active sites, try starting one by clicking 'new site'.
- rescue => e
  - pp e
