.row.bottom-10
  .span8
    .panel
      .inner
        %h2 Active Sites
        = link_to 'New Site', new_site_path, :class => "btn"
        %p
          - current_user.sites.each do |site|
            %li
              %strong Name of Site:
              = site.name
        %p
        = render 'dashboard/sites'
